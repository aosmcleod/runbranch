---
name: A stack that will not configure
about: You could not describe your project in a .runbranch config
labels: config
---

**The stack** — package manager, framework, what starts it, what it needs running

**What `./runbranch.sh propose <repo>` produced**

```
```

**What is missing from the config format to describe it**

This is the most useful kind of report: the config format is meant to describe
any project that runs locally, and every stack it cannot describe is a gap.
