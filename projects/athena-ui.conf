NAME="athena-ui"
REPO="~/Development/athena-ui"
DEFAULT_BRANCH="main"
INSTALL="npm ci"
TARGETS="docs:5173:/:npm run docs -- --port {port}"
OPENS_ITSELF=1

# Sidebar glyph (SF Symbols name).
SYMBOL="paintpalette"

# Honour the repo pin inside the worktree, not the launcher directory.
RUNTIME="fnm"
