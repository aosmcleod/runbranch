NAME="Bookstand"
REPO="~/Development/Bookstand"
DEFAULT_BRANCH="master"
INSTALL="pnpm install --frozen-lockfile"
COPY_FILES=".env.local"
TARGETS="web:3000:/:pnpm dev"

# Sidebar glyph (SF Symbols name).
SYMBOL="folder"
